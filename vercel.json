{
  "installCommand": "corepack use pnpm@10.13.1 && pnpm --prefix chat install --frozen-lockfile",
  "buildCommand": "pnpm --prefix chat build",
  "outputDirectory": "chat/.next",
  "framework": "nextjs"
}
